version: 2

seeds:
  - name: raw_customers
    config:
      schema: DEV_WAREHOUSE
  - name: raw_items
    config:
      schema: DEV_WAREHOUSE
  - name: raw_orders
    config:
      schema: DEV_WAREHOUSE
  - name: raw_products
    config:
      schema: DEV_WAREHOUSE
  - name: raw_stores
    config:
      schema: DEV_WAREHOUSE
  - name: raw_supplies
    config:
      schema: DEV_WAREHOUSE